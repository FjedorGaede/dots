-- Hyprland Lua Configuration
-- Migrated from hyprlang to Lua for Hyprland >= 0.55
-- https://wiki.hypr.land/Configuring/Start/

require("shared")        -- Colors, fonts, wallpaper, variables
require("monitors")      -- Monitor setup
require("appearance")    -- General, decoration, dwindle, misc
require("animations")    -- Curves and animation settings
require("input")         -- Keyboard, mouse, touchpad, gestures
require("autostart")     -- Startup applications
require("keybinds")      -- Keybindings and submaps
require("window-rules")  -- Window rules and workspace rules
